name = "codex-openai-wrapper"
main = "src/index.ts"
compatibility_date = "2024-09-23"

compatibility_flags = ["nodejs_compat"]

preview_urls = false

# --- KV Namespaces ---
# KV namespaces are injected at build/dev time via
# scripts/prepare-wrangler-config.mjs to avoid committing IDs.
# Do not define kv_namespaces here to allow generation without validation errors.


# --- Local Development Configuration ---
# This section configures local development with Docker and miniflare
[dev]
ip = "0.0.0.0"          # Bind to all interfaces for Docker access
port = 8787             # Port for local development server
# Note: --persist-to flag in Docker handles storage location

# wrangler.toml (wrangler v3.88.0^)
[observability.logs]
enabled = true
